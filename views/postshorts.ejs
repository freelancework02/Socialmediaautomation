<% if (!short) { %>

<div class="text-center text-white">
  <h1 class="text-3xl mb-4 font-bold text-cyan-400">
    No YouTube Shorts Pending 🎉
  </h1>
  <p class="text-gray-400">All shorts are uploaded already.</p>
</div>

<% } else { %>

<h1 class="text-3xl text-cyan-400 mb-4 font-bold">YouTube Shorts Preview</h1>

<div class="bg-slate-800 p-6 rounded-xl shadow-xl">

  <video controls class="w-full rounded mb-4">
      <source src="<%= mainUrl %>" type="video/mp4">
  <source src="<%= fallbackUrl %>" type="video/mp4">
  </video>

  <p class="text-white mb-2">
    <span class="text-cyan-400">🎯 Title:</span>
    <%= short.title || "No Title" %>
  </p>

  <p class="text-white mb-4">
    <span class="text-cyan-400">📝 Description:</span>
    <%= short.description || "No Description" %>
  </p>

  <form action="/upload-youtube" method="POST">
    <button class="bg-red-500 px-6 py-3 rounded text-lg hover:bg-red-400">
      Upload to YouTube Shorts 🚀
    </button>
  </form>

</div>

<% } %>
